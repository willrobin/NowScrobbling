# Copy to .deploy.env and adjust for your setup.
# Example:
# NS_DEPLOY_CMD='rsync -av --delete nowscrobbling/ user@server:/var/www/wp-content/plugins/nowscrobbling/'

NS_DEPLOY_CMD='echo replace-with-real-deploy-command'
